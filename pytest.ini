[pytest]
testpaths = tests
addopts = -ra --strict-markers
markers =
    unit: fast unit tests with no external services
    integration: tests that exercise multiple local components
    network: tests allowed to perform network access when explicitly requested
    slow: tests that are expected to take longer than the default suite
